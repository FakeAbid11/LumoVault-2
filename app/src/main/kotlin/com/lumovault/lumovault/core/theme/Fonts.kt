package com.lumovault.lumovault.core.theme

import androidx.compose.ui.text.font.FontFamily

/**
 * Roboto is Android's system typeface, so the platform default is the brand
 * font — no bundled font asset is needed, and this stays correct if a device
 * ships a different default. The original Dart pinned `Roboto` by name for the
 * same reason.
 */
val RobotoFontFamily: FontFamily = FontFamily.Default
